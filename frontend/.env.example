# AIverse Frontend Environment Variables
# Copy this file to .env and fill in your actual values

# ===========================================
# API CONFIGURATION
# ===========================================
# Backend API URL - this is where your backend server is running
# For local development, this should point to your backend server
VITE_API_URL=http://localhost:5000

# Alternative environment variable names (for compatibility)
VITE_PUBLIC_API_URL=http://localhost:5000
VITE_BACKEND_URL=http://localhost:5000

# ===========================================
# PRODUCTION CONFIGURATION
# ===========================================
# For production deployments, update these URLs to your deployed backend
# Example production URLs:
# VITE_API_URL=https://your-backend-domain.com
# VITE_PUBLIC_API_URL=https://your-backend-domain.com
# VITE_BACKEND_URL=https://your-backend-domain.com

# ===========================================
# DEVELOPMENT NOTES
# ===========================================
# The frontend will automatically detect the environment and use:
# - Development: http://localhost:5000 (or your VITE_API_URL)
# - Production: https://aiverse-sbs6.onrender.com (fallback)

# You can also set the API URL via meta tag in index.html:
# <meta name="backend-base-url" content="http://localhost:5000">

# Or via window object in your HTML:
# <script>window.__VITE_API_URL = 'http://localhost:5000';</script>

# ===========================================
# TESTING
# ===========================================
# For testing with the backend, make sure your backend is running on the specified port
# Default backend port: 5000
# Default frontend port: 5173 (Vite dev server)
